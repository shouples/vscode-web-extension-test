{
    "name": "Go Development",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "golang.go",
                "shouples-dev.dev-web-extension"
            ],
            "settings": {
                "terminal.integrated.shell.linux": "/bin/bash",
                "go.gopath": "/go"
            }
        }
    }
}